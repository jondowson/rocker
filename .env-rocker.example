# ============================================================
# .env-rocker - Remote Development Configuration
# ============================================================
# All configuration for remote Docker development workflow
# Used by: rocker, rocker-headless scripts
# ============================================================

# --- DOCKER CREDENTIALS ---
# Get an access token from: https://hub.docker.com/settings/security
DOCKER_USERNAME='name'
DOCKER_PASSWORD='password'

# --- REMOTE HOST CONFIGURATION ---
REMOTE_HOST=zermatt                    # SSH alias or hostname (e.g., zermatt, zermatt.local)
REMOTE_USER=                           # Optional: SSH username (leave empty if in ~/.ssh/config)

# --- LOCAL PROJECT PATHS ---
LOCAL_ROOT_DIR=~/Development           # Root directory for projects
LOCAL_NAMESPACE=Barkley                # Subdirectory to search first

# --- SSH TUNNEL CONFIGURATION ---
# Space-separated list of port forwards (local:remote:port format)
TUNNEL_PORTS="5173:localhost:5173 3000:localhost:3000"

# --- HEADLESS MODE DEFAULTS ---
# Sleep settings when disabling headless mode (in minutes)
NORMAL_SLEEP=10
NORMAL_DISPLAY_SLEEP=10
NORMAL_DISK_SLEEP=10
